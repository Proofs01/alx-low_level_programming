#include "main.h"
#include <ctype.h>

/**
 * _isdigit - determine if digit
 *
 * @c: character to determine
 *
 * Return: return 0 on success and 1 on fail
 */

int _isdigit(int c)
{
  int i = isdigit(c);

  if (i > 0 )
  {

	      return (1);
       return (0);
}
